steps:
  # 1. Build the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/user-management-api:$COMMIT_SHA
      - -t
      - gcr.io/$PROJECT_ID/user-management-api:latest
      - .

  # 2. Run tests inside the container (uses SQLite, no DB needed)
  - name: "gcr.io/cloud-builders/docker"
    args:
      - run
      - --rm
      - --entrypoint
      - pytest
      - gcr.io/$PROJECT_ID/user-management-api:$COMMIT_SHA
      - tests/
      - --tb=short

  # 3. Push image to Container Registry
  - name: "gcr.io/cloud-builders/docker"
    args:
      - push
      - gcr.io/$PROJECT_ID/user-management-api:$COMMIT_SHA

  - name: "gcr.io/cloud-builders/docker"
    args:
      - push
      - gcr.io/$PROJECT_ID/user-management-api:latest

  # 4. Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - run
      - deploy
      - user-management-api
      - --image=gcr.io/$PROJECT_ID/user-management-api:$COMMIT_SHA
      - --region=us-central1
      - --platform=managed
      - --allow-unauthenticated
      - --set-env-vars=DATABASE_URL=$$DATABASE_URL
    secretEnv:
      - DATABASE_URL

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/DATABASE_URL/versions/latest
      env: DATABASE_URL

images:
  - gcr.io/$PROJECT_ID/user-management-api:$COMMIT_SHA
  - gcr.io/$PROJECT_ID/user-management-api:latest

options:
  logging: CLOUD_LOGGING_ONLY
